<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="main.css" />
  </head>
  <body class="grey-bg">
    <%- include('nav.ejs') %>
    <!-- 2번 째 인자로는 ejs에 값 넘겨줄 수 있다 -->

    <input class="search" />
    <button class="search-send">검색</button>

    <script>
      document.querySelector(".search-send").addEventListener("click", (e) => {
        let inputtedVal = document.querySelector(".search").value;
        location.href = `/search?val=${inputtedVal}`; // val라는 변수에 inputtedVal를 저장해서 보낸다.
      });
    </script>

    <div class="white-bg">
      <% for (let i = 0; i < result.length; i++){ %>
      <div class="list-box">
        <h4>
          <a
            href="/detail/<%= result[i]._id %>"
            style="text-decoration: none; color: black"
            ><%= result[i].title %></a
          >
          <!-- <img src="<%= result[i].img %>" height="50px" alt="x" /> -->
          <!-- 수정 태그 -->
          <a href="/edit/<%= result[i]._id %>">🖍️</a>
          <span class="delete" data-id="<%= result[i]._id %>">🗑️</span>
        </h4>
        <p><%= result[i].content %></p>
      </div>
      <% } %>
    </div>

    <a href="/list/next/<%= result[result.length - 1]._id %>">next</a>

    <script>
      for (let i = 0; i < "<%= result.length%>"; i++) {
        document
          .querySelectorAll(".delete")
          [i].addEventListener("click", (e) => {
            fetch("/delete?docid=" + e.target.dataset.id, { method: "DELETE" })
              .then((r) => r.text())
              .then((r) => {
                e.target.parentElement.parentElement.style.display = "none";
              });
          });
      }

      // document.querySelectorAll(".delete")[0].addEventListener("click", (e) => {
      //   e.target.dataset.id;
      //   fetch("/delete?docid=<%= result[0]._id %>", { method: "DELETE" });
      // });
    </script>
  </body>
</html>
